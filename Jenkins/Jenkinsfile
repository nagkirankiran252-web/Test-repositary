pipeline {
    agent any

    stages {
        stage('System Information') {
            steps {
                sh 'hostname'
            }
        }
        stage('Server Date Details ') {
            steps {
                sh 'date'
            }
        }
        stage('CPU Usage Details') {
            steps {
                sh 'lscpu'
            }
        }
    }
}
